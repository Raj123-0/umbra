---
phase: "03"
plan: "03"
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - tests/test_rubin_pooling.py
  - benchmarks/misspecification_benchmark.py
autonomous: true
requirements:
  - POOL-04
---

# Plan 03-03: Multiple Imputation Coverage Verification & Benchmark

## Goal
Verify that Rubin pooling ($M \ge 5$) with Barnard-Rubin (1999) small-sample adjustment achieves nominal coverage ($\ge 90\%$) and provides valid inference compared to single plug-in imputation, which undercovers due to ignoring between-imputation variance.

## Proposed Work

<task id="03-03-01" type="auto">
  <name>Author unit tests for Rubin pooling, Barnard-Rubin DOF, and multi-draw interface</name>
  <read_first>
    - umbra/imputers/rubin_pooler.py
    - tests/test_coverage.py
  </read_first>
  <acceptance_criteria>
    - `tests/test_rubin_pooling.py` contains:
      - `test_transform_multiple_all_imputers`: verifies `transform_multiple(X, m=5)` across all 4 stochastic imputers produces 5 distinct datasets with no missing values.
      - `test_rubin_pooler_scalar_and_vector`: tests pooling on scalar and vector estimates.
      - `test_barnard_rubin_adjustment_bounds`: verifies $\nu_{\text{adj}} \le \nu_0$ when sample size is small, and $\nu_{\text{adj}} \to \nu_m$ when $\nu_0 \to \infty$.
      - `test_rubin_pooler_downstream_model_fit`: verifies `RubinPooler` fitting LinearRegression across $M$ DataFrames.
      - `test_rubin_pooled_ci_wider_than_single_plugin`: proves between-imputation variance strictly widens confidence interval compared to naive single plug-in imputation.
      - `test_rubin_pooler_fmi_and_efficiency`: verifies FMI and relative efficiency calculations.
    - All tests pass, 0 type errors, 0 linter issues.
  </acceptance_criteria>
  <action>
    Create `tests/test_rubin_pooling.py`.
    Optionally update `benchmarks/misspecification_benchmark.py` to support multi-imputation pooling evaluations.
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_rubin_pooling.py -v</automated>
  </verify>
</task>
